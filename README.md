# Vava Living Website

This repository contains the safe, version-controlled WordPress application code for the Vava Living website.

## Deployment notes

Create `wp-config.php` on the target server from `wp-config-sample.php` and provide the database credentials through the server environment or a private deployment mechanism. User uploads and generated media are intentionally excluded from this repository; restore them separately when needed.
